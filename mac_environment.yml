# macOS-specific environment
# Includes base dependencies and adds macOS-specific packages

name: seafloor_geodesy_mac
channels:
  - defaults
  - conda-forge

dependencies:
  # Include base shared dependencies
  - base_environment.yml
  
  # macOS-specific packages
  - gfortran
  - clang
